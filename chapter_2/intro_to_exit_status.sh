#!/bin/bash

# Write a shell script that displays "This script will exit with a 0 exit 
# staus." Be sure that the script does indeed exit with a 0 status

echo "This script will exit with a 0 status"
exit 0